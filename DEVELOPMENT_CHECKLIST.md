# AI 恋爱大师智能体开发进度清单

> 最后更新：2026-07-20
> 当前分支：`codex/upgrade-spring-ai-1-1`
> 当前阶段：Spring AI 1.1.2 升级重构已完成并验证，下一阶段开发可演示的后端 API。

## 状态说明

- `[x]` 已完成并经过测试
- `[ ]` 尚未完成
- `进行中` 已有代码，但还需要审阅、提交或补齐验收
- `暂缓` 当前阶段不做，避免扩大开发范围

## 一、当前能力总览

| 模块 | 状态 | 已具备的能力 | 下一步缺口 |
| --- | --- | --- | --- |
| 基础聊天 | 已完成 | DashScope/Qwen 调用、多轮会话 | 暴露 REST/SSE 接口 |
| 结构化输出 | 已完成 | 生成 `LoveReport` 标题和建议列表 | 增加字段校验与前端 DTO |
| 对话记忆 | 已完成 | 文件持久化、10 条消息窗口、会话隔离、原子写入 | 会话查询/清理接口，后期可换数据库 |
| Advisor | 已完成 | 隐私友好的调用日志、ReReading/Re2 | 增加耗时、Token 和工具调用追踪 |
| RAG | 已完成基础版 | 查询改写、pgvector、TopK=5、阈值 0.5、稳定文档 ID、增量导入 | 引用来源、质量评测、生产级索引策略 |
| Tool Calling | 已完成基础版 | 文件读写工具、百度网页搜索工具 | 增加统一结果协议、权限与执行轨迹 |
| MCP | 未开始 | 已明确使用高德地图 MCP | 接入 POI、周边搜索和路线规划 |
| ReAct Agent | 未开始 | 当前由 ChatClient 自动进行工具调用 | 增加显式任务编排、状态和最大步数 |
| 约会计划 | 未开始 | 可复用搜索与后续地图工具 | 聚合地点、路线、预算、时间和备选方案 |
| PDF 文档 | 未开始 | 文件工具可保存文本 | 生成并返回完整约会方案 PDF |
| 后端 API | 未开始 | 目前只有健康检查接口 | REST、SSE、异常响应和会话接口 |
| 前端 | 暂缓 | 尚未开发 | 后端主流程稳定后再实现 |

## 二、已确定的核心设计边界

### 智能体不是让模型决定所有事情

后续会增加统一的 Agent 编排入口，但不同能力的控制方式不同：

- 固定基础能力：会话记忆、日志、安全校验、超时和调用次数限制由程序默认执行，不交给模型自由决定。
- 模型可选能力：网页搜索、地图/MCP、天气和知识库检索可以作为工具，由模型根据任务选择。
- 业务输出方式：普通文本、`LoveReport`、`DatePlan` 或 PDF 通常由用户操作和后端流程决定，不完全依赖模型猜测。

当前 `LoveApp` 的普通聊天、报告、RAG 和 Tool Calling 方法分开，便于学习、测试和对比效果；后续再由 Agent 编排层组合这些能力。

### RAG 当前入库行为与后续目标

- 当前每次创建相关 Spring 容器时会重新读取和切分 Markdown，并检查确定性分块 ID。
- 已存在的分块会跳过，只对缺失分块调用 Embedding 并入库；不是每个测试方法都重新生成全部向量。
- 当前增量导入不会自动删除文档修改前的旧向量，因此仍属于学习用基础版。
- 后续使用“文档来源 + 内容哈希 + Embedding 模型版本 + 切分配置版本”判断是否重新向量化，文档变更或删除时同步清理旧向量。
- 知识库索引最终应与聊天应用启动解耦，由独立服务、管理接口或后台任务执行更新。

## 三、阶段 0：升级分支收尾（P0，已完成）

### 已完成

- [x] 提交升级前快照：`c6b4715 feat: add initial tool calling support`
- [x] 创建升级分支：`codex/upgrade-spring-ai-1-1`
- [x] 升级到 Java 21、Spring Boot 3.5.14、Spring AI 1.1.2
- [x] 升级到 Spring AI Alibaba 1.1.2.2
- [x] 使用 Spring AI、Spring AI Alibaba 和 Extensions BOM 管理版本
- [x] 修复 Maven Wrapper，Windows 可直接运行 `mvnw.cmd`
- [x] 提供无密钥的 `application-local.example.yml`
- [x] 将 API Key 改为通过环境变量或本地忽略配置注入
- [x] 迁移聊天、报告、Memory、Advisor、RAG 和 Tool Calling API
- [x] 默认测试不调用真实 DashScope、PostgreSQL 或 SearchAPI
- [x] `mvnw.cmd clean test` 通过：22 个测试、0 失败、3 个外部测试跳过
- [x] 真实普通聊天、结构化报告、RAG 和 Tool Calling 冒烟验证通过
- [x] 修复向量库建表与知识库导入的生命周期顺序，仅写入缺失的确定性文档 ID
- [x] 兼容阿里云 ARM RDS：使用独立表和精确检索，保留 HNSW 为可配置选项
- [x] 完成功能链路审阅，确认 ChatClient、Memory、Advisor、RAG 和 Tool Calling 的实际行为
- [x] 将升级重构、测试和开发文档作为一个完整的本地提交保存

### 后续仓库整理

- [ ] 在阿里云控制台轮换历史中曾经硬编码的 DashScope Key
- [ ] 决定是否重写 Git 历史以彻底清除旧 Key
- [ ] 更新项目 README：环境准备、配置方式、启动命令、测试命令和架构说明

### 阶段验收标准

- 干净克隆后仅配置环境变量即可编译和启动。
- `mvnw.cmd clean test` 稳定通过。
- Git 中不新增任何密钥、运行时记忆文件或临时文件。
- 升级改动经过审阅并提交后，再进入下一阶段。

## 四、阶段 1：提供可演示的后端 API（P1，下一优先级）

### 开发项

- [ ] 新增普通聊天 REST 接口
- [ ] 新增结构化报告接口
- [ ] 新增 RAG 聊天接口
- [ ] 新增 Tool Calling 聊天接口
- [ ] 新增 SSE 流式聊天接口
- [ ] 设计统一请求 DTO：`message`、`conversationId`、聊天模式
- [ ] 设计统一响应 DTO 和全局异常处理
- [ ] 增加参数校验、错误码和 Trace ID
- [ ] 增加清除会话记忆接口
- [ ] 增加会话 ID 列表或会话摘要接口
- [ ] 补充 Knife4j/Swagger 接口说明和调用示例
- [ ] 使用 MockMvc 完成接口测试

### 阶段验收标准

- Postman 或 Swagger 可以调用四种聊天模式。
- 相同 `conversationId` 能记住上下文，不同会话相互隔离。
- SSE 能逐段返回内容，并能正确处理客户端断开。
- 缺少参数、模型超时和工具失败时返回统一错误结构。

## 五、阶段 2：Agent 与高德 MCP 核心能力（P2，项目核心亮点）

### Agent 编排

- [ ] 定义 Agent 输入：城市、日期、预算、偏好、交通方式和忌口等约束
- [ ] 划分固定基础能力、模型可选工具和业务控制的输出格式
- [ ] 引入 ReAct/ReactAgent，并明确“思考—行动—观察”的执行循环
- [ ] 设置最大执行步数、总超时和重复工具调用检测
- [ ] 保存每一步的工具名、输入、结果摘要、耗时和状态
- [ ] 工具失败时支持重试、降级或向用户补问
- [ ] 对危险或越权工具操作增加确认机制
- [ ] 将最终回答与内部执行轨迹分离，避免向用户暴露内部提示词
- [ ] 将知识库检索封装为可选工具，并与“每次固定 RAG”方案做效果、耗时和成本对比

### 高德地图 MCP

- [ ] 配置 Spring AI MCP Client
- [ ] 接入高德地点关键词搜索
- [ ] 接入附近地点搜索
- [ ] 接入地点详情查询
- [ ] 接入驾车、步行或公共交通路线规划
- [ ] 统一封装 MCP 超时、空结果和服务异常
- [ ] 使用 Mock MCP Server 完成离线测试
- [ ] 使用真实高德 MCP 完成受控冒烟测试

### 阶段验收标准

- 用户说“周六在上海，预算 500 元，安排一次安静的约会”后，Agent 能主动补齐必要信息。
- Agent 能自主选择网页搜索和高德 MCP，并在有限步数内完成任务。
- 执行过程可追踪，工具失败不会导致整个服务无响应。

## 六、阶段 3：完整约会计划与文档交付（P3）

- [ ] 设计结构化 `DatePlan`：时间段、地点、路线、预算、注意事项和备选方案
- [ ] 综合网页搜索、地图地点、路线和用户约束生成计划
- [ ] 对营业时间、距离、预算和路线冲突进行二次校验
- [ ] 为天气或地点关闭准备 Plan B
- [ ] 为检索结果保留来源链接和更新时间
- [ ] 生成 Markdown/HTML 版本约会计划
- [ ] 增加 PDF 生成工具
- [ ] 返回文档 ID、文件地址或下载接口
- [ ] 加固 PDF 文件名、保存路径、生命周期和访问权限
- [ ] 增加端到端测试：用户需求 → Agent → MCP/工具 → 结构化计划 → PDF

### 阶段验收标准

- 一次请求能产出可执行的半日或一日约会安排。
- 地点顺序、路线时间和预算基本合理，并提供来源与备选方案。
- 用户可以获得结构化 JSON 和 PDF 两种结果。

## 七、阶段 4：RAG、评测与工程化完善（P4）

### RAG 完善

- [ ] 将知识库索引与应用启动解耦，避免每次启动都调用 Embedding
- [ ] 使用文档版本或内容哈希实现增量索引，文档更新/删除时清理对应旧向量
- [ ] Embedding 模型、向量维度或切分规则改变时执行版本化重建
- [ ] 增加文档分类和元数据过滤：单身、恋爱、已婚
- [ ] 在答案中返回引用文档及片段来源
- [ ] 构建恋爱咨询问题评测集
- [ ] 评估 Recall@K、答案相关性、忠实度和无答案处理
- [ ] 根据评测结果调整分块、TopK 和相似度阈值

### 可观察性与可靠性

- [ ] 记录模型名称、耗时、Token 用量和调用结果，不记录完整隐私内容
- [ ] 记录 RAG 检索数量、相似度和命中文档 ID
- [ ] 记录工具/MCP 调用成功率、耗时和失败类型
- [ ] 增加模型、数据库、搜索和 MCP 的超时与重试策略
- [ ] 增加限流、并发控制和简单熔断
- [ ] 增加健康检查：数据库、向量库和外部工具可用性

### 安全与业务边界

- [ ] 增加敏感信息脱敏和日志审计
- [ ] 增加提示词注入与恶意工具参数防护
- [ ] 增加自伤、暴力、跟踪骚扰等高风险内容识别和升级提示
- [ ] 明确产品不是心理治疗或紧急救助服务
- [ ] 增加会话数据保留期限和删除机制
- [ ] 面向多用户时增加鉴权和会话归属校验

## 八、阶段 5：前端与部署（后置）

- [ ] 聊天页面和会话列表
- [ ] SSE 流式输出和停止生成
- [ ] 展示 Agent 当前步骤和工具调用状态
- [ ] 展示地点卡片、地图、路线和预算
- [ ] 展示 RAG 引用来源
- [ ] 下载约会计划 PDF
- [ ] Docker Compose 编排应用与 PostgreSQL/pgvector
- [ ] 配置开发、测试和生产环境
- [ ] 增加 CI：编译、单测、密钥扫描和构建镜像
- [ ] 部署演示环境

## 九、当前明确暂缓的内容

- [ ] Spring AI 2.x / Spring Boot 4：作为后续独立实验，不影响当前主项目
- [ ] 复杂多 Agent 协作：先完成一个可靠的 ReAct Agent
- [ ] 完整用户系统和支付系统：与当前 Agent 学习目标无关
- [ ] 大规模分布式架构：当前先保证单体后端可测试、可演示

## 十、推荐的实际开发顺序

1. 完成 REST、SSE、统一异常处理和会话管理接口。
2. 接入高德 MCP，先独立验证地点搜索和路线规划。
3. 实现有最大步数和执行轨迹的 ReAct Agent。
4. 完成“约会需求 → 工具/MCP → 结构化计划”的主流程。
5. 增加 PDF 输出。
6. 建立 RAG 与 Agent 评测集，再优化检索和提示词。
7. 最后开发前端、Docker 和部署演示环境。

## 十一、简历与面试可讲解点

- Spring AI M6 到稳定版 1.1.2 的 API 和依赖迁移。
- 使用 `ChatMemoryRepository + MessageWindowChatMemory` 实现可替换的持久化记忆。
- 使用原子文件替换和会话级锁处理并发写入。
- 使用查询改写、pgvector、阈值过滤和确定性文档 ID 构建 RAG。
- 处理 DashScope Embedding 单批最多 10 条和 1536 维配置等真实兼容问题。
- 使用新版 Advisor API 实现 ReReading，并避免日志泄露用户隐私。
- 使用 `ToolCallback` 注册文件和网页搜索工具，并处理路径穿越、大小限制、超时和错误响应。
- 通过 Mock ChatModel、Mock HTTP 和临时目录让默认测试不依赖外部服务。
- 后续重点讲解 ReAct Agent 如何选择工具、高德 MCP 如何接入，以及如何约束执行步数和处理失败。
